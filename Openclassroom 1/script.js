const motApplication = "Codepen"

let mot_user = prompt("Veuillez entrer le mot : " + motApplication);
if (motApplication === mot_user) {
    alert("Bravo!");
    
} else {
    alert("Erreur de frappe!");
}
